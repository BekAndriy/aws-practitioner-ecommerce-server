{
  "name": "authorization-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint:fix": "eslint src --fix",
    "rm:dist": "node -e \"fs.rmSync('./dist', { recursive: true, force: true });\"",
    "build": "npm run build:prod",
    "build:code": "npm run rm:dist && tsc -p tsconfig.json",
    "build:prod": "npm run lint:fix && npm run build:code && npm run build:modules",
    "build:modules": "node ./scripts/buildModules",
    "deploy": "sls deploy",
    "build:deploy": "npm run build && npm run deploy"
  },
  "author": "Andriy Bek",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.124",
    "@typescript-eslint/eslint-plugin": "^6.7.5",
    "eslint": "^8.51.0",
    "eslint-config-standard-with-typescript": "^39.1.1",
    "eslint-plugin-import": "^2.28.1",
    "eslint-plugin-n": "^16.2.0",
    "eslint-plugin-promise": "^6.1.1",
    "serverless-dotenv-plugin": "^6.0.0",
    "typescript": "^5.2.2"
  }
}