{
  "name": "products-service",
  "version": "1.0.0",
  "description": "Products service",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:prod",
    "build:prod": "npm run lint:fix && npm run test && npm run rm:dist && tsc -p tsconfig.prod.json && npm run build:modules",
    "build:modules": "node ./scripts/buildModules",
    "rm:dist": "node -e \"fs.rmSync('./dist', { recursive: true, force: true });\"",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "lint:doc": "swagger-cli validate openapi/openapi.yml",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:lambda": "serverless invoke local --function functionNameFromTheServerless.yml",
    "deploy": "sls deploy",
    "deploy:doc": "sls s3deploy",
    "remove": "sls remove"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.122",
    "@types/jest": "^29.5.5",
    "@types/node": "^20.7.1",
    "@typescript-eslint/eslint-plugin": "^6.7.3",
    "eslint": "^8.50.0",
    "eslint-config-standard-with-typescript": "^39.1.0",
    "eslint-plugin-import": "^2.28.1",
    "eslint-plugin-n": "^16.1.0",
    "eslint-plugin-promise": "^6.1.1",
    "jest": "^29.7.0",
    "prettier": "^3.0.3",
    "serverless": "^3.35.2",
    "serverless-s3-cleaner": "^2.0.2",
    "serverless-s3-deploy": "^0.10.1",
    "swagger-cli": "^4.0.4",
    "ts-jest": "^29.1.1",
    "ts-loader": "^9.4.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "yup": "^1.3.2"
  }
}