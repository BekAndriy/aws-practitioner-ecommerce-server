{
  "name": "import-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "rm:dist": "node -e \"fs.rmSync('./dist', { recursive: true, force: true });\"",
    "build:code": "npm run rm:dist && tsc -p tsconfig.json",
    "deploy": "sls deploy",
    "build:deploy": "npm run build:code && npm run deploy"
  },
  "author": "Andriy Bek",
  "license": "ISC",
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.427.0",
    "@aws-sdk/s3-request-presigner": "^3.427.0",
    "@types/aws-lambda": "^8.10.124",
    "@types/jest": "^29.5.5",
    "aws-lambda": "^1.0.7",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "csv-parse": "^5.5.2",
    "csv-parser": "^3.0.0"
  }
}
